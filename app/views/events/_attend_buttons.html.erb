<% if attends? %>
  <%= button_to "Do not attend", event_remove_attendee_path(@event), class: "button is-medium is-warning" %>
<% else %>
  <%= button_to "Attend", event_add_attendee_path(@event), class: "button is-medium is-success" %>
<% end %>