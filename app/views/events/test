<div class="field">
  <div class="control">
    <%= f.label :start_date, class: "label" %>
    <div class="select is-primary is-normal is-grouped">
      <%= f.datetime_select :start_date, {:minute_step => 15, order: [:day,
      :month, :year], value: @current_time, start_date: Date.today}, {class: ""}
      %>
    </div>
  </div>
</div>

<div class="field">
  <div class="control">
    <%= f.label :end_date, class: "label" %> <%= f.datetime_select :end_date,
    :minute_step => 15, selected: Date.today, default: Date.today.year, order:
    [:day, :month, :year], class: "input is-medium" %>
  </div>
</div>
